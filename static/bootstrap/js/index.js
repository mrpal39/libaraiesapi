{
    /* < script >
        alert("thia") <
        /script>


    // var len = response.length;
    // for (var i = 0; i < len; i++) {
    //     var dependent_repos_count = response[i].dependent_repos_count;
    //     var dependents_count = response[i].dependents_count;
    //     var description = response[i].description;
    //     var forks = response[i].forks;

    //     var tr_str = "<tr>" + // "
    // <
    // td align = 'center' > " + dependent_repos_count + " < /td>" + / / " <
    // td align = 'center' > " + dependents_count + " < /td>" + / / " <
    // td align = 'center' > " + description + " < /td>" + / / " <
    // td align = 'center' > " + forks + " < /td>" + / / "</tr>"; // $("#userTable tbody").append(tr_str); // } $("#platform").hide(); } }); }); $("a").click(function() { var platform = $("a").val(); alert(platform); }); }); */
}